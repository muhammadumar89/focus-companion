# personal data — your ratings, requests & viewing history (stays on your machine)
taste.json
ratings.jsonl
requests.json

# logs
*.log

# local-only nightly wrapper (does the git push; not part of the shared product)
nightly.sh

# backups, caches, OS cruft
*.bak
__pycache__/
.DS_Store
